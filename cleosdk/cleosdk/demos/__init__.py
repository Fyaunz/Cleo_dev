# cleosdk/demos/__init__.py
"""Runnable example clients, e.g. `python -m cleosdk.demos.demo_move`.

They import the installed `cleosdk` package like any other consumer would, so
they double as a check that the public API is usable from outside the SDK.
"""
